import React from "react";
import { ContainerLoading, LoadingSpinner } from "./styles";

export default function Loading() {
    return (
      <ContainerLoading>
        <LoadingSpinner />
      </ContainerLoading>
    );

}
